package co.simplon.masterpiece.controllers;

import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RestController;

@RestController
@CrossOrigin("*")
public class ServerAttributeController {
	/*
	 * MOVED TO SERVER CONTROLLER, DELETE IF TESTS WELL private ServerAttributeService
	 * service;
	 * 
	 * public ServerAttributeController(ServerAttributeService service) { this.service =
	 * service; }
	 * 
	 * @GetMapping("/serverAttributeNames") protected List<String> server() { return
	 * service.getNames(); }
	 */

}
